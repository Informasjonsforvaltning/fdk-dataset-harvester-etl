# Default values for fdk-dataset-harvester-etl.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

##fdk-dataset-harvester-etl
FDK_DATASET_HARVESTER_ETL_MEMORY: "1000Mi"